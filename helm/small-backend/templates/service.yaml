apiVersion: v1
kind: Service
metadata:
  name: {{ include "small-backend.fullname" . }}
  labels:
    {{- include "small-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "small-backend.selectorLabels" . | nindent 4 }}
